# 卡片：介绍 Spark（1-2min）
Spark 的目标是高效处理海量数据，核心可以抽象为四件事：何时触发任务、从哪里读数据、如何处理数据、把结果写到哪里。它类似 SQL 查询，但数据来源是分布式存储，计算是并行执行，通常由调度系统触发。Spark 应用由 Driver 和 Executor 组成，Driver 负责构建 DAG 和调度，Executor 在各节点执行具体计算并回传或写出结果。这个结构让大规模数据处理灵活且高效。
