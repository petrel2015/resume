# 卡片：Spark 内存管理（1-2min）
Spark 的内存管理核心是避免 OOM，同时尽量把资源用满。Executor 内存分堆内和堆外，堆内由统一内存管理，执行内存和存储内存共享同一池子，比例可以调。执行内存主要给 shuffle、join 这类计算用，存储内存用于缓存和广播，紧张时可以落盘。开启堆外内存后，部分数据结构会放到 off-heap，GC 压力会小一些。实际调优要结合作业特征和 GC 指标平衡执行和缓存。
